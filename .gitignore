# Banco local — 40+ MB, binário, reconstruível em ~2 min pelo coletor.
# O que vai versionado é o snapshot padronizado em dados/ (CSV + JSON).
*.db
*.db-wal
*.db-shm

__pycache__/
*.py[cod]
.venv/
venv/
.env

.streamlit/secrets.toml
.vscode/
.DS_Store
desktop.ini

# Configuracao local do Claude Code / VS Code
.claude/
Claude/

# Saida da camada de publicacao. O modulo georisk_publicar.py existe e
# funciona, mas o site NAO esta no ar: enquanto estiver dormente, nao faz
# sentido versionar 285 KB de JSON regerado a cada 3 h. Para acionar, veja a
# secao "Camada de publicacao" do README.
site/api/

# Log da coleta local agendada (Agendador do Windows)
coleta_local.log
